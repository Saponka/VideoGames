/*  const axios = require('axios')
const {APIKEYY} = process.env
const URL= 'https://api.rawg.io/api/genres'

const infoGeneros= async()=>{
    const picar = await axios.get(`${URL}?key=${APIKEYY}`)
    const data = picar.data.results
    return data
}

module.exports={
    infoGeneros,
}  */